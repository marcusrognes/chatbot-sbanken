version: '2'
services:
  mongodb:
      image: mongo:latest
      container_name: "mongodb"
      volumes:
        - ./data/db:/data/db
      ports:
          - 27017:27017
  redis:
    image: "redis:alpine"
    ports:
          - 6379:6379